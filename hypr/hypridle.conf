general {
    lock_cmd = pidof hyprlock || hyprlock
    unlock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch sddm on
    ignore_dbus_inhibit = false 
    ignore_systemd_inhibit = false
}
